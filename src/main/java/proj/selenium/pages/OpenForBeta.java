package proj.selenium.pages;

import proj.selenium.lib.Util;
import proj.selenium.lib.Web;
import proj.selenium.utils.WebDriverWrapper;


public class OpenForBeta {
    /*public MainPage mainPage;
    public SignUpPage signUpPage;
    public LogginPage logginPage;

    public Web web;
    public Util util;


    public OpenForBeta(WebDriverWrapper driver) throws Exception {

        mainPage = new MainPage(driver);
        logginPage = new LogginPage(driver);
        signUpPage = new SignUpPage(driver);

        util = new Util(driver);
        web  = new Web(driver);
    }*/

}
